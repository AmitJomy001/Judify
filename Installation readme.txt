1. Press windows button + R simultaneously.
2. type cmd and press enter
3. type the following codes/copy paste them one by one.
These are libraries which are essential to run the application.
pip install youtube_dl
pip install pygame
pip install tkinter

4. if any error "No module named "X" appears, repeat the step
using pip install X in cmd 

5. Finally to run the program.
Goto Program folder and run by double clicking Master

Thanks for the support!!!